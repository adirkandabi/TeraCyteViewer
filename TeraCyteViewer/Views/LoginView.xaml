<UserControl x:Class="TeraCyteViewer.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="40">
        <StackPanel FlowDirection="LeftToRight" Width="320">
            <TextBlock Text="Sign In" FontSize="26" FontWeight="Bold" Margin="0 0 0 16"/>
            <TextBlock Text="Username"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Password" Margin="0 12 0 0"/>
            <PasswordBox x:Name="Pwd"/>

            <Button Content="Sign In" Margin="0 16 0 0" Padding="8"
              Click="Login_Click"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Tomato" Margin="0 8 0 0"/>
        </StackPanel>
    </Grid>
</UserControl>
